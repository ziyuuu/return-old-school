name: Archived Batch01 bootstrap notice
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Batch01 source is already readable on main. Use m11b-b01-build.yml; do not reapply seed transports.'
