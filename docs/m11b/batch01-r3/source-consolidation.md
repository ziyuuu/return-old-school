# R3 源码接续与冲突保存

提交当前工作期间，main从R2归档0b000a17前进到11c70ba6。逐文件比较共4个提交：增加两个b01-r3-source传输分片、一份不同接口的R3 core，以及将R2工作流改为手动。

本轮不强推或回退main。合并以11c70ba6为第一父提交、R3完整实现b880720c为第二父提交；保留两个传输分片与R2手动工作流。此前新增的core完整保存在本目录archive/preintegration-core.mjs.txt（原blob fa4414ca9592987c400bd84028df8aca51285cfc），避免丢失任何已提交工作。

运行时选择带完整参数、渲染适配、105项本轮单元/反例、导出与浏览器QA脚本的统一版本（entrances/central参数，recentered prism polygon）。两种草案的API和多边形坐标约定不同，不能把另一份core与此渲染器拼接，也不能直接重放早期bundle覆盖当前源码。

共同的历史关系不变：两端正面退入、旁边五层高墙向前延续，中央浅弧；不重做已获认可校园基底。局部轮廓H以当前input及真实浏览器审阅结果为准。工程尚未浏览器验收通过时，不称为完成。
